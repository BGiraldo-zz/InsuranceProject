@model IEnumerable<Insurance.Domain.AggregatesModel.PolicyDetailAggregate.PolicyDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.CompleteName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Policy.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.CompleteName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Policy.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Change Status", "Edit", new { id=item.PolicyDetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PolicyDetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PolicyDetailId })
        </td>
    </tr>
}

</table>
